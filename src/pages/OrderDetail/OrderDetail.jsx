import NavBar from "../../components/NavBar/NavBar"
export default function OrderDetail({user,setUser}) {
    return(
        <main className="OrderDetail">
            <NavBar />
        </main> 
    )
}